import test from 'node:test';
import assert from 'node:assert/strict';
import {studentSummary,studyHistory,validateExamQuestions} from '../dist/mentoring.js';
const p={id:'s',role:'student'};
const data={profiles:[p],cycles:[{id:'c',student_id:'s',status:'Publicado',start_date:'2020-01-01',duration_days:30,blocks:[{id:'b',date:'2020-01-08',method:{steps:[{id:'study',kind:'study',title:'Teoria'}]}}]}],progress:[{student_id:'s',key:'c|position',flags:{day:8}}],method_studies:[],method_reviews:[{student_id:'s',cycle_id:'c',status:'pending',due_day:7},{student_id:'s',cycle_id:'c',status:'pending',due_day:8},{student_id:'other',cycle_id:'c',status:'pending',due_day:1}],study_logs:[{student_id:'s',topic_key:'key',subject:'Matemática',topic:'MMC',date:'2026-09-13',origin:'timer',session_id:'timer',minutes:67,total:0,correct:0},{student_id:'s',topic_key:'key',subject:'Matemática',topic:'MMC',date:'2026-09-13',origin:'method',minutes:0,total:20,correct:15},{student_id:'s',topic_key:'key',subject:'Matemática',topic:'MMC',date:'2026-09-04',origin:'external',minutes:30,total:10,correct:5}],study_sessions:[{id:'timer',student_id:'s',topic_key:'key',subject:'Matemática',topic:'MMC',started_at:'2026-09-13T12:00:00Z',heartbeat_at:'2026-09-13T13:07:00Z',focused_seconds:4020}]};
test('acompanhamento mantém posição sequencial, usa proporção ponderada e separa revisões',()=>{const m=studentSummary(data,p,'2026-09-14');assert.equal(m.day,8);assert.equal(m.length,30);assert.equal(m.overdue,1);assert.equal(m.due,2);assert.equal(m.total,30);assert.equal(m.correct,20);assert.equal(m.seconds,4020);assert.equal(m.trend,25);assert.equal(m.last,'2026-09-13');assert.equal(m.pending,1);assert.equal(m.level.label,'Atenção');});
test('histórico reúne questões e tempo sem somar duas vezes o Pomodoro',()=>{const rows=studyHistory(data,'s');assert.equal(rows.length,2);assert.equal(rows[0].seconds,4020);assert.equal(rows[0].manualMinutes,0);assert.equal(rows[0].total,20);assert.equal(rows[0].correct,15);assert.equal(rows[1].manualMinutes,30);assert.deepEqual(studyHistory(data,'other'),[]);});
test('limite de simulado permite 80 e rejeita 81',()=>{assert.equal(validateExamQuestions(Array(80)).length,80);assert.throws(()=>validateExamQuestions(Array(81)),/^Error: Um simulado pode ter no máximo 80 questões\.$/);});
